
export const locales = ['en', 'sq'] as const;
export const defaultLocale = 'en';

// localeLoaderConfig, LocaleConfigType, and getLocaleConfig are removed from here
// as localeLoaderConfig will be defined directly in client.ts.
